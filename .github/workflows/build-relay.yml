name: Build Relay

on:
  schedule:
    - cron: '0 4 * * *'
  repository_dispatch:
    types: [php-updated]
  workflow_dispatch:
    inputs:
      php_versions:
        description: 'PHP versions (comma-separated, empty=all)'
        type: string
      force_build:
        description: 'Force build'
        type: boolean
        default: false

jobs:
  build:
    uses: ./.github/workflows/_build-extension.yml
    with:
      extension: relay
      packagist: cachewerk/ext-relay
      display_name: Relay
      brew_deps: ""
      php_versions: ${{ inputs.php_versions || github.event.client_payload.php_versions || '' }}
      force_build: ${{ inputs.force_build || false }}
    secrets: inherit
